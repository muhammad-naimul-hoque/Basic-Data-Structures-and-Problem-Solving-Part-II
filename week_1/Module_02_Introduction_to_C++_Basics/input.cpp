#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a, b;
    // cin >> a >> b;
    // cout << a << " " << b << endl;

    char ch[50];
    // cin.getline(ch, 50);
    // cout << ch << endl;

    int c[5];
    for (int i = 0; i < 5; i++)
    {
        cin >> c[i];
    }

    for (int i = 0; i < 5; i++)
    {
        cout << c[i] << " ";
    }

    return 0;
}