#include <bits/stdc++.h>
using namespace std;

int main()
{
    string st = "abcdefghij";
    // string::iterator it;
    // string::reverse_iterator it;

    for (auto it = st.begin(); it != st.end(); it++)
    {
        cout << *it << endl;
    }

    return 0;
}